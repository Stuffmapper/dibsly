<!DOCTYPE html>
<html>
<head>

  <title>Stuffmapper</title>



  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/png" href="/assets/favicon.png" />
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC9wZTqNMPxl86PtJuR4Dq3TzS_hByOs3U"
  type="text/javascript"></script>
  <%= stylesheet_link_tag    "application", media: "all" %>

    <% if @omniuser %>
      <%= javascript_tag do %>
        localStorage.setItem('sMToken', JSON.stringify(<%= raw @omniuser.to_json %>) )
      <% end %>
    <% end %>

  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
   <base href="/"> </base>

</head>

  <body ng-app="stfmpr" ng-controller="MainNavCtrl as mainnav">
      <ng-view></ng-view>
    <script type="text/javascript">
    var _sf_async_config={uid:57522,domain:"stuffmapper-dev.herokuapp.com",useCanonical:true};
    (function(){
      function loadChartbeat() {
        window._sf_endpt=(new Date()).getTime();
        var e = document.createElement('script');
        e.setAttribute('language', 'javascript');
        e.setAttribute('type', 'text/javascript');
        e.setAttribute('src', '//static.chartbeat.com/js/chartbeat.js');
        document.body.appendChild(e);
      }
      var oldonload = window.onload;
      window.onload = (typeof window.onload != 'function') ?
         loadChartbeat : function() { oldonload(); loadChartbeat(); };
    })();
    </script>
  </body>
</html>
