<div id="map-canvas" style="width: 100%; height: 100%; margin: 0px; top: 0px; left: 0px; overflow: hidden; z-index: 1; position: absolute; background-color: rgb(229, 227, 223);"></div>
<script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?sensor=true"></script>

<div id="main-grid">
  <div id="grid-post-container">
    <% @posts.each do |post| %>
      <div class="grid-post">
        <%= image_tag post.image.url(:medium) %><br>
        <%= post.title %>
        <div class="grid-post-details">
          <div class="grid-post-description"><%= post.description %></div>
          <div class="grid-post-address"><%= post.address %></div>
          <span class="grid-post-date">Posted <%= distance_of_time_in_words(post.created_at, Time.now) %> ago</span>
          <%= link_to dib_post_path(post), method: :dib, :title => "Dibs" do %>
            <image src="assets/dibs.png" class="dibs-image"></image>
          <% end %><br>
        </div>
      </div>
    <% end %>
  </div>
  <%= paginate @posts %>
</div>
